[package]
name = "full-tree"
version = "0.1.0"
edition = "2024"

[dependencies]
mylib1 = { path = "libs/mylib1" } # a local package

# Outside code/rs, members would be listed here:
# [workspace]
# members = ["libs/mylib1"]
